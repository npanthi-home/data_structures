package tree.bst.redblack;

import common.Mutator;

public class ColourFixer<T extends Comparable<? super T>> implements Mutator<T, RedBlackTree<T>> {
    @Override
    public RedBlackTree<T> mutate(RedBlackTree<T> node) {
        if(node != null) {

        }
        return null;
    }
}
